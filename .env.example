# Restaurant POS Frontend - Environment Variables
# Copy this file to .env.local and fill in your values

# API - Express.js backend URL (update when backend team provides endpoint)
NEXT_PUBLIC_API_URL=http://localhost:3001/api

# Optional: Enable API route rewrites for same-origin requests during development
# NEXT_PUBLIC_USE_PROXY=false
